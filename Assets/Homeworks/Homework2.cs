//Homework2
//When character passes through trigger, instantiate something.
//Implement run function in SimpleMove.
//Optional 1: while character in trigger, instantiate the same thing


using UnityEngine;

public class Homework2 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
